import React from "react";
import { JsxElement } from "typescript";

type Props = {};

// Component for users to input new post to be created

const Post = (props: Props) => {
  return <>Post</>;
};

export default Post;
